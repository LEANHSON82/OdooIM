<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="cron_assignment_reminder" model="ir.cron">
            <field name="name">eLearning: nhắc khoá học sắp đến hạn</field>
            <field name="model_id" ref="im_elearning.model_im_course_assignment"/>
            <field name="state">code</field>
            <field name="code">model._cron_remind_deadline()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall"
                   eval="(DateTime.now().replace(hour=7, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>

        <record id="cron_assignment_overdue" model="ir.cron">
            <field name="name">eLearning: báo khoá học quá hạn</field>
            <field name="model_id" ref="im_elearning.model_im_course_assignment"/>
            <field name="state">code</field>
            <field name="code">model._cron_notify_overdue()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall"
                   eval="(DateTime.now().replace(hour=7, minute=30, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>
