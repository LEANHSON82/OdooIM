<?xml version="1.0"?>
<!-- Giao diện và hành động backend để cấu hình SLA policy helpdesk. -->
<odoo>

    <record id="helpdesk_sla_view_tree" model="ir.ui.view">
        <field name="name">helpdesk.sla.list</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <list string="SLA Policies" multi_edit="1" default_order="team_id,name">
                <field name="name"/>
                <field name="team_id"/>
                <field name="priority"/>
                <field name="stage_id"/>
                <field name="time" widget="float_time"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="partner_ids" widget="many2many_tags" optional="hide"/>
                <field name="ticket_count" readonly="1"/>
                <field name="active" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="helpdesk_sla_view_kanban" model="ir.ui.view">
        <field name="name">helpdesk.sla.kanban</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <kanban sample="1">
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold fs-5"/>
                        <div><field name="team_id"/></div>
                        <div class="text-muted">
                            <field name="time" widget="float_time"/> to reach <field name="stage_id"/>
                        </div>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="helpdesk_sla_view_form" model="ir.ui.view">
        <field name="name">helpdesk.sla.form</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <form string="SLA Policy">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_helpdesk_ticket" type="object" class="oe_stat_button" icon="fa-life-ring" invisible="ticket_count == 0">
                            <field name="ticket_count" string="Tickets" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Urgent tickets"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="team_id" required="1"/>
                            <field name="priority" widget="priority"/>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            <field name="partner_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="stage_id" domain="[('team_ids', 'in', team_id)]"/>
                            <field name="exclude_stage_ids" widget="many2many_tags" domain="[('team_ids', 'in', team_id), ('id', '!=', stage_id)]"/>
                            <field name="time" widget="float_time"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="helpdesk_sla_view_search" model="ir.ui.view">
        <field name="name">helpdesk.sla.search</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <search string="SLA Policies">
                <field name="name"/>
                <field name="team_id"/>
                <field name="stage_id"/>
                <field name="tag_ids"/>
                <field name="partner_ids"/>
                <filter string="Archived" name="archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Team" name="team_id" context="{'group_by': 'team_id'}"/>
                    <filter string="Priority" name="priority" context="{'group_by': 'priority'}"/>
                    <filter string="Target Stage" name="stage_id" context="{'group_by': 'stage_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="helpdesk_sla_action_main" model="ir.actions.act_window">
        <field name="name">SLA Policies</field>
        <field name="res_model">helpdesk.sla</field>
        <field name="path">helpdesk-sla-policies</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="search_view_id" ref="helpdesk_sla_view_search"/>
        <field name="context">{'active_test': False}</field>
    </record>

</odoo>
